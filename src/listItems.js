import React, { useState, useEffect, useRef } from "react";

function listItems(){

    return(
        <section class="listOfItems">
            <p>This is a list of items present in the closet. Click on an item to see where you have kept them.</p>
        </section>
    );
}

export default listItems;